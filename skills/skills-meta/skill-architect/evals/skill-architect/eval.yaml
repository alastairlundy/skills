name: skill-architect
description: Evaluates skill-architect skill trigger discipline for designing new agent skills
skill: skill-architect
version: "1.0"

config:
  trials_per_task: 3
  timeout_seconds: 300
  parallel: false
  executor: mock
  model: gpt-5-mini

tasks:
  - "tasks/*.yaml"
