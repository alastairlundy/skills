id: trigger-technical-default
name: Trigger Technical Default
description: >-
  Anti-slop should trigger by default for technical/analytical requests.
tags:
  - trigger
  - positive-trigger
inputs:
  prompt: |
    Design a caching strategy for our API.
expected:
  should_trigger: true
graders:
  - type: trigger
    name: trigger-grader
    config:
      skill_path: skills/alignment/anti-slop
      mode: positive
      threshold: 0.5
