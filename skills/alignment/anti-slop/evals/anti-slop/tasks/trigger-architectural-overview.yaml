id: trigger-architectural-overview
name: Trigger Architectural Overview
description: >-
  Anti-slop should trigger for architectural overview requests (exempt from measurability only).
tags:
  - trigger
  - positive-trigger
inputs:
  prompt: |
    Give me an architectural overview of the order processing system.
expected:
  should_trigger: true
graders:
  - type: trigger
    name: trigger-grader
    config:
      skill_path: skills/alignment/anti-slop
      mode: positive
      threshold: 0.5
