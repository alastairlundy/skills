id: perf-dependency-graph
name: Perf Dependency Graph
description: >-
  >-
tags:
  - performance
inputs:
  prompt: |
    Decompose this spec into tickets:
expected:
  output_contains:
    - "Blocked by"
    - "Dependencies - Blocked by"
  output_not_contains:
    - "Blocks"
graders:
  - type: text
    name: assertion-checks
    config:
      not_contains:
        - "Blocks"
      contains:
        - "Blocked by"
        - "Dependencies - Blocked by"
