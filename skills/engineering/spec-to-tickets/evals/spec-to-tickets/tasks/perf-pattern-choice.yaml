id: perf-pattern-choice
name: Perf Pattern Choice
description: >-
  >-
tags:
  - performance
inputs:
  prompt: |
    Decompose this spec into tickets:
expected:
  output_contains:
    - "perf-pattern-choice"
graders:
  - type: text
    name: assertion-checks
    config:
      contains:
        - "perf-pattern-choice"
