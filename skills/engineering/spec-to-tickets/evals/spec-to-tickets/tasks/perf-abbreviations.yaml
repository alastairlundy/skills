id: perf-abbreviations
name: Perf Abbreviations
description: >-
  >-
tags:
  - performance
inputs:
  prompt: |
    Decompose this spec into tickets:
expected:
  output_contains:
    - "SSO (Single Sign-On)"
    - "User Stories"
graders:
  - type: text
    name: assertion-checks
    config:
      contains:
        - "SSO (Single Sign-On)"
        - "User Stories"
