id: no-trigger-domain-modeling
name: No trigger on domain modeling
description: Does NOT trigger for a domain modeling or terminology alignment question.
inputs:
  prompt: |
    What's the bounded context for orders vs invoices in a multi-tenant SaaS?
expected:
  should_trigger: false
graders:
  - type: trigger
    name: should-not-trigger-code-implementation-grilling
    config:
      skill_path: skills/engineering/code-implementation-grilling
      mode: negative
      threshold: 0.4
