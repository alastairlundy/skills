# Dependency Review Skill

## Roadmap

### dependency-review

Capabilities deferred from the current skill:

- **v2**: cross-checked replacement recommendations — the skill currently surfaces "this dependency looks problematic" without proposing a drop-in replacement
- **v2/v3**: reflection-based and convention-based implicit-dependency detection
- **Future**: a CLI that consumes the Guide and extracts the deterministic steps (Steps 1, 2) — to be opened as a new branch when the deterministic gaps in the Skill-only path are documented
